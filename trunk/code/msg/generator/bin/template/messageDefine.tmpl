//code generated by generator don't edit.
using Google.Protobuf;

namespace EngineCore
{
    public static class MessageDefine
	{
        @foreach (var m in Model) 
        {
        @:public const int @(m.MessageType) = @(m.MessageId);
        }

        public static int GetMessageId(this IMessage message)
        {
        	int messageId = 0;
        	switch (message.Descriptor.Name)
        	{
        		@foreach(var m in Model){
        		@:case "@(m.MessageType)":
        			@:messageId = @(m.MessageId);
        			@:break;
        		}
        		default:
        			break;
        	}

        	return messageId;
    	}
    }
}

//code generated by generator don't edit.
using Google.Protobuf;

namespace EngineCore
{
	public static class MessageTargetDefine
	{
		@foreach (var m in Model) 
		{
		@:public const string @(m.MessageType)= "@(m.MessageTarget)";
		}

		public static string GetServerUrl(IMessage message)
		{
			string serverUrl = string.Empty;
			string messageDescriptorName = message.Descriptor.Name;

			switch(messageDescriptorName)
			{
			@foreach(var m in Model)
			{
				@:case "@(m.MessageType)":
					@:serverUrl = "@(m.MessageTarget)";
					@:break;
			}
				default:
					break;
			}
			return serverUrl;
		}
	}
}
